# Question 47: Explain the `try`, `catch`, and `throw` keywords.

**Explain the `try`, `catch`, and `throw` keywords.**
    - **Answer/Explanation:** `try` encloses code that might throw exceptions, `catch` handles them, `throw` raises exceptions.
    - **Code Example:** See above.
    - **Key Concepts:** Exception flow.
    - **Problem-Solving Technique:** Error recovery.